import 'package:flutter/material.dart';

class BottomNavChallenge extends StatelessWidget {
  const BottomNavChallenge({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bottom Nav')),
      body: const Center(child: Text('Challenge goes here')),
    );
  }
}
